// const filterReducer = (state = '', { type, payload }) => {
//     switch (type) {     
//         case 'filter/change':
//             return { ...state, filter: payload }
        
//         default: return state;
//     }
// };

// filterReducer()